<UserControl x:Class="WpfApp1.ProductDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="300">
    <Border x:Name="MainBorder" CornerRadius="10" Background="{Binding BackgroundColor}">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Image Grid.Row="0" Source="{Binding ImageUrl}" Stretch="Uniform"/>
            <TextBlock Grid.Row="1" Text="{Binding Description}" Foreground="White" FontSize="16" Margin="0,10,0,0" TextWrapping="Wrap"/>
            <TextBlock Grid.Row="2" Text="{Binding Price, StringFormat=C}" Foreground="White" FontSize="16" Margin="0,10,0,0"/>
        </Grid>
    </Border>
</UserControl>
